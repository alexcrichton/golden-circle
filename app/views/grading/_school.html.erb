<tr class="<%= school_counter % 2 == 0 ? 'even' : 'odd' %>">
  <td class="num"><%= @schools_rank[school_counter] %></td>
  <td><%= school.name %></td>
  <td class="num"><%= school.apprentice_team.team_score %></td>
  <td class="num"><%= school.wizard_team.team_score %></td>
  <td class="num"><%= school.school_score %></td>
</tr>
